import { prisma } from './backend/src/config/db'; async function main() { const user = await prisma.user.findFirst({ where: { name: { contains: 'Moni', mode: 'insensitive' } } }); if (!user) return; const notifs = await prisma.notification.findMany({ where: { userId: user.id, type: 'achievement' }, orderBy: { sentAt: 'desc' } }); console.log('NOTIFS:', JSON.stringify(notifs, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
