import { prisma } from './backend/src/config/db'; async function main() { const achs = await prisma.achievement.findMany(); console.log('ACHS:', achs); const users = await prisma.user.findMany({ where: { name: { contains: 'Moni', mode: 'insensitive' } }, include: { achievements: { include: { achievement: true } } } }); console.log('USERS:', JSON.stringify(users, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
